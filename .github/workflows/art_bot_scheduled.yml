name: Art Bot Scheduled 🎨

on:
  schedule:
    # АРТ: 9:00, 14:00, 19:00, 21:00 МСК (6, 11, 16, 18 UTC)
    - cron: '0 6,11,16,18 * * *'
    # ВИДЕО: Воскресенье 22:00 МСК (19 UTC)
    - cron: '0 19 * * 0'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Post mode (art or video)'
        required: true
        default: 'art'
        type: choice
        options:
          - art
          - video

jobs:
  post_art:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Runner Time Info
        run: |
          echo "Date: $(date)"
          echo "DOW: $(date +%u) (1-Mon, 7-Sun)"
          echo "Hour: $(date +%H)"

      - name: Run Art Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          GOOGLE_KEY: ${{ secrets.GOOGLE_KEY }}
          SILICONFLOW_KEY: ${{ secrets.SILICONFLOW_KEY }}
          RUNWARE_KEY: ${{ secrets.RUNWARE_KEY }}
          KIE_KEY: ${{ secrets.KIE_KEY }}
          HF_KEY: ${{ secrets.HF_KEY }}
          CLOUDFLARE_ID: ${{ secrets.CLOUDFLARE_ID }}
          CLOUDFLARE_TOKEN: ${{ secrets.CLOUDFLARE_TOKEN }}
          GROQ_KEY: ${{ secrets.GROQ_KEY }}
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
          LAOZHANG_KEY: ${{ secrets.LAOZHANG_KEY }}
          YOUR_SIGNATURE: "@fRieNDLee34"
        run: |
          DOW=$(date +%u)
          HOUR=$(date +%H)
          
          # Если это ручной запуск ВИДЕО или воскресенье 19:00 UTC (22:00 MSK)
          if [ "${{ github.event.inputs.mode }}" == "video" ] || ([ "$DOW" -eq 7 ] && [ "$HOUR" -ge 18 ] && [ "$HOUR" -le 20 ]); then
            echo "🎬 Running Video Generation..."
            python3 -u main.py --video 2>&1 | tee bot_run.log
          else
            echo "🖌️ Running Art Generation..."
            python3 -u main.py 2>&1 | tee bot_run.log
          fi
